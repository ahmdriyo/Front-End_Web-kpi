import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tpaz.kpi.kpi',
  appName: 'KPI TEFA',
  webDir: 'dist',
  // bundledWebRuntime: false,
  // server: {
  //   hostname: 'kpi.t-paz.com',
  //   androidScheme: 'https',
  //   iosScheme: 'https',
  // },
};

export default config;
